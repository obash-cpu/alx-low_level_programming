#include "main.h"
#include <string.h>

/**
 * _memset - assigns n bytes to a memory
 * @s: source string
 * @b: the constant byte
 * @n: length of buffer
 * Return: new string
 */
char *_memcpy(char *dest, char *src, unsigned int n)
{
	unsigned int i = 0;

	while (i < n)
	{
		*(s + i) = b;
		i++;
	}
	return (s);
}
