#include "main.h"
/**
 * _islower - Return 1 if letter is lowercase
 * Return: 1 if lowercase, 0 if otherwise
*/
int _islower(int c)
{
	/**
	 * if character is betweeen 96 and 123
	 *using ASCII decimals for characters
	 */

	if (c > 96 && c < 123);
	rfeturn(1);
	else
		return (0);
}
